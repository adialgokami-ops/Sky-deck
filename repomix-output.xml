This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  app/
    admin/
      page.tsx
    api/
      admin/
        verify/
          route.ts
    fonts/
      GeistMonoVF.woff
      GeistVF.woff
    favicon.ico
    globals.css
    layout.tsx
    page.tsx
  components/
    BookingModal.tsx
    TableCard.tsx
  hooks/
    useAutoExpiry.ts
    useRealtimeBookings.ts
    useRealtimeTables.ts
  lib/
    supabase.ts
    types.ts
.eslintrc.json
.gitignore
next.config.mjs
package.json
postcss.config.mjs
PROJECT_GUIDE.txt
README.md
seed.sql
skydeck-table-booking-antigravity-prompt.md
tailwind.config.ts
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/app/admin/page.tsx">
'use client';

import { useState, useEffect, useCallback } from 'react';
import {
  Lock,
  CheckCircle2,
  XCircle,
  Phone,
  Users,
  Clock,
  ChevronDown,
  ChevronUp,
  MessageSquare,
  LogOut,
} from 'lucide-react';
import { supabase } from '@/lib/supabase';
import { useRealtimeTables } from '@/hooks/useRealtimeTables';
import { useRealtimeBookings } from '@/hooks/useRealtimeBookings';
import { useAutoExpiry } from '@/hooks/useAutoExpiry';
import { ZONES, EXPIRY_MINUTES } from '@/lib/types';
import type { Table, TableStatus, Booking } from '@/lib/types';
import TableCard from '@/components/TableCard';

/** Admin table status cycle: available → occupied → cleaning → available */
const ADMIN_STATUS_CYCLE: Record<TableStatus, TableStatus> = {
  available: 'occupied',
  occupied: 'cleaning',
  cleaning: 'available',
  pending: 'available', // clicking a pending table releases it
};

export default function AdminPage() {
  const [isAuthed, setIsAuthed] = useState(false);
  const [pin, setPin] = useState('');
  const [pinError, setPinError] = useState('');
  const [pinLoading, setPinLoading] = useState(false);
  const [showHistory, setShowHistory] = useState(false);

  // Check sessionStorage on mount
  useEffect(() => {
    if (typeof window !== 'undefined') {
      const stored = sessionStorage.getItem('skydeck_admin');
      if (stored === 'true') setIsAuthed(true);
    }
  }, []);

  const handlePinSubmit = useCallback(async () => {
    setPinLoading(true);
    setPinError('');

    try {
      const res = await fetch('/api/admin/verify', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ pin }),
      });

      if (res.ok) {
        // This is demo-only security, not production-grade.
        sessionStorage.setItem('skydeck_admin', 'true');
        setIsAuthed(true);
      } else {
        setPinError('Invalid PIN. Try again.');
      }
    } catch {
      setPinError('Network error. Try again.');
    } finally {
      setPinLoading(false);
    }
  }, [pin]);

  const handleLogout = useCallback(() => {
    sessionStorage.removeItem('skydeck_admin');
    setIsAuthed(false);
    setPin('');
  }, []);

  if (!isAuthed) {
    return <PinScreen
      pin={pin}
      setPin={setPin}
      error={pinError}
      loading={pinLoading}
      onSubmit={handlePinSubmit}
    />;
  }

  return <AdminDashboard
    showHistory={showHistory}
    setShowHistory={setShowHistory}
    onLogout={handleLogout}
  />;
}

// ── PIN Entry Screen ──────────────────────────────────────────────

function PinScreen({
  pin,
  setPin,
  error,
  loading,
  onSubmit,
}: {
  pin: string;
  setPin: (v: string) => void;
  error: string;
  loading: boolean;
  onSubmit: () => void;
}) {
  return (
    <div className="flex min-h-screen items-center justify-center bg-[#0F0F12] px-4">
      <div className="pointer-events-none fixed inset-0 bg-gradient-to-b from-amber-900/8 via-transparent to-transparent" />

      <div className="relative w-full max-w-sm rounded-3xl border border-white/10 bg-[#1a1a22]/80 p-8 shadow-2xl backdrop-blur-md text-center">
        <div className="mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full bg-amber-500/10">
          <Lock size={28} className="text-amber-400" />
        </div>
        <h1 className="font-serif text-2xl font-bold text-white mb-2">
          Staff Access
        </h1>
        <p className="text-sm text-white/40 mb-6">
          Enter admin PIN to continue
        </p>

        <input
          type="password"
          value={pin}
          onChange={(e) => setPin(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && onSubmit()}
          placeholder="Enter PIN"
          className="mb-4 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-center text-lg tracking-[0.3em] text-white placeholder:text-white/30 focus:border-amber-500/50 focus:outline-none focus:ring-1 focus:ring-amber-500/30 transition-colors"
          autoFocus
        />

        {error && (
          <p className="mb-4 text-sm text-red-400">{error}</p>
        )}

        <button
          onClick={onSubmit}
          disabled={!pin || loading}
          className="w-full rounded-xl bg-gradient-to-r from-amber-600 to-amber-500 px-6 py-3 font-semibold text-white shadow-lg shadow-amber-500/20 transition-all hover:shadow-amber-500/40 disabled:opacity-40 disabled:cursor-not-allowed active:scale-[0.98]"
        >
          {loading ? 'Verifying…' : 'Unlock'}
        </button>
      </div>
    </div>
  );
}

// ── Admin Dashboard ───────────────────────────────────────────────

function AdminDashboard({
  showHistory,
  setShowHistory,
  onLogout,
}: {
  showHistory: boolean;
  setShowHistory: (v: boolean) => void;
  onLogout: () => void;
}) {
  const { tables } = useRealtimeTables();
  const { bookings } = useRealtimeBookings();
  const pendingBookings = bookings.filter((b) => b.status === 'pending');
  const historyBookings = bookings
    .filter((b) => ['confirmed', 'cancelled', 'expired'].includes(b.status))
    .slice(0, 10);

  // Run auto-expiry
  useAutoExpiry();

  const cycleTableStatus = useCallback(async (table: Table) => {
    const nextStatus = ADMIN_STATUS_CYCLE[table.status];
    await supabase
      .from('tables')
      .update({ status: nextStatus, updated_at: new Date().toISOString() })
      .eq('id', table.id);
  }, []);

  const confirmBooking = useCallback(async (booking: Booking) => {
    await supabase
      .from('bookings')
      .update({ status: 'confirmed' })
      .eq('id', booking.id);

    await supabase
      .from('tables')
      .update({ status: 'occupied', updated_at: new Date().toISOString() })
      .eq('id', booking.table_id);
  }, []);

  const releaseBooking = useCallback(async (booking: Booking) => {
    await supabase
      .from('bookings')
      .update({ status: 'cancelled' })
      .eq('id', booking.id);

    await supabase
      .from('tables')
      .update({ status: 'available', updated_at: new Date().toISOString() })
      .eq('id', booking.table_id);
  }, []);

  return (
    <div className="min-h-screen bg-[#0F0F12]">
      <div className="pointer-events-none fixed inset-0 bg-gradient-to-b from-amber-900/8 via-transparent to-transparent" />

      <div className="relative mx-auto max-w-5xl px-4 pb-8">
        {/* Header */}
        <header className="flex items-center justify-between pb-6 pt-6">
          <div>
            <h1 className="font-serif text-3xl font-bold text-white">
              Sky<span className="text-amber-400">Deck</span>
              <span className="ml-3 text-sm font-sans font-normal text-white/30">
                Staff Panel
              </span>
            </h1>
          </div>
          <button
            onClick={onLogout}
            className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-sm text-white/50 transition-colors hover:bg-white/10 hover:text-white"
          >
            <LogOut size={14} />
            Sign Out
          </button>
        </header>

        {/* ── Section 1: Live Table Grid ── */}
        <section className="mb-10">
          <h2 className="mb-4 font-serif text-xl font-bold text-white/80">
            Live Table Grid
          </h2>

          {ZONES.map((zone) => {
            const zoneTables = tables.filter((t) => t.zone === zone);
            if (zoneTables.length === 0) return null;

            return (
              <div key={zone} className="mb-6">
                <h3 className="mb-3 text-sm font-medium uppercase tracking-wider text-white/30">
                  {zone}
                </h3>
                <div className="grid grid-cols-3 gap-3 sm:grid-cols-4 md:grid-cols-6">
                  {zoneTables.map((table) => (
                    <TableCard
                      key={table.id}
                      table={table}
                      adminMode
                      onClick={() => cycleTableStatus(table)}
                    />
                  ))}
                </div>
              </div>
            );
          })}
        </section>

        {/* ── Section 2: Request Queue ── */}
        <section className="mb-10">
          <h2 className="mb-4 font-serif text-xl font-bold text-white/80 flex items-center gap-2">
            Request Queue
            {pendingBookings.length > 0 && (
              <span className="rounded-full bg-amber-500/20 px-2.5 py-0.5 text-xs font-semibold text-amber-400">
                {pendingBookings.length}
              </span>
            )}
          </h2>

          {pendingBookings.length === 0 && (
            <div className="rounded-2xl border border-white/5 bg-white/[0.02] p-8 text-center">
              <p className="text-white/30">No pending requests</p>
            </div>
          )}

          <div className="space-y-3">
            {pendingBookings.map((booking) => (
              <BookingRequestCard
                key={booking.id}
                booking={booking}
                onConfirm={() => confirmBooking(booking)}
                onRelease={() => releaseBooking(booking)}
              />
            ))}
          </div>
        </section>

        {/* ── Section 3: Recent History (collapsible) ── */}
        <section>
          <button
            onClick={() => setShowHistory(!showHistory)}
            className="mb-4 flex w-full items-center justify-between rounded-2xl border border-white/5 bg-white/[0.02] px-5 py-3 text-left transition-colors hover:bg-white/[0.04]"
          >
            <h2 className="font-serif text-lg font-bold text-white/50">
              Recent History
            </h2>
            {showHistory ? (
              <ChevronUp size={18} className="text-white/30" />
            ) : (
              <ChevronDown size={18} className="text-white/30" />
            )}
          </button>

          {showHistory && (
            <div className="space-y-2">
              {historyBookings.length === 0 && (
                <p className="py-4 text-center text-sm text-white/25">No recent activity</p>
              )}
              {historyBookings.map((booking) => (
                <HistoryCard key={booking.id} booking={booking} />
              ))}
            </div>
          )}
        </section>
      </div>
    </div>
  );
}

// ── Booking Request Card ──────────────────────────────────────────

function BookingRequestCard({
  booking,
  onConfirm,
  onRelease,
}: {
  booking: Booking;
  onConfirm: () => void;
  onRelease: () => void;
}) {
  const [countdown, setCountdown] = useState('');

  useEffect(() => {
    const update = () => {
      const created = new Date(booking.created_at).getTime();
      const expiresAt = created + EXPIRY_MINUTES * 60 * 1000;
      const remaining = Math.max(0, Math.floor((expiresAt - Date.now()) / 1000));
      const m = Math.floor(remaining / 60);
      const s = remaining % 60;
      setCountdown(`${m}:${s.toString().padStart(2, '0')}`);
    };

    update();
    const interval = setInterval(update, 1000);
    return () => clearInterval(interval);
  }, [booking.created_at]);

  const tableLabel = booking.tables?.label ?? '—';
  const timeSince = getTimeSince(booking.created_at);

  return (
    <div className="rounded-2xl border border-amber-500/20 bg-amber-500/5 p-4 backdrop-blur-sm">
      <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div className="flex-1 min-w-0">
          <div className="flex items-center gap-3 mb-2">
            <span className="font-serif text-lg font-bold text-white">
              {booking.guest_name}
            </span>
            <span className="rounded-lg bg-white/5 px-2 py-0.5 text-xs font-bold text-amber-400 border border-amber-500/20">
              {tableLabel}
            </span>
          </div>

          <div className="flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-white/50">
            <a
              href={`tel:+91${booking.phone}`}
              className="flex items-center gap-1 text-amber-400/80 hover:text-amber-400 transition-colors"
            >
              <Phone size={13} />
              +91 {booking.phone}
            </a>
            <span className="flex items-center gap-1">
              <Users size={13} />
              {booking.party_size} guests
            </span>
            <span className="flex items-center gap-1">
              <Clock size={13} />
              {timeSince}
            </span>
          </div>

          {booking.note && (
            <p className="mt-1.5 flex items-start gap-1 text-sm text-white/40 italic">
              <MessageSquare size={13} className="mt-0.5 shrink-0" />
              {booking.note}
            </p>
          )}
        </div>

        <div className="flex items-center gap-2 shrink-0">
          {/* Countdown */}
          <span className={`font-mono text-sm font-bold mr-2 ${
            countdown.startsWith('0:') || countdown.startsWith('1:') ? 'text-red-400' : 'text-white/40'
          }`}>
            {countdown}
          </span>

          <button
            onClick={onConfirm}
            className="flex items-center gap-1.5 rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white shadow-lg shadow-emerald-600/20 transition-all hover:bg-emerald-500 hover:shadow-emerald-500/30 active:scale-[0.97]"
          >
            <CheckCircle2 size={16} />
            Confirm &amp; Seat
          </button>
          <button
            onClick={onRelease}
            className="flex items-center gap-1.5 rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm font-medium text-white/60 transition-all hover:bg-red-500/10 hover:text-red-400 hover:border-red-500/30 active:scale-[0.97]"
          >
            <XCircle size={16} />
            Release
          </button>
        </div>
      </div>
    </div>
  );
}

// ── History Card ──────────────────────────────────────────────────

function HistoryCard({ booking }: { booking: Booking }) {
  const tableLabel = booking.tables?.label ?? '—';
  const statusColor =
    booking.status === 'confirmed'
      ? 'text-emerald-400 bg-emerald-500/10 border-emerald-500/20'
      : booking.status === 'cancelled'
      ? 'text-red-400 bg-red-500/10 border-red-500/20'
      : 'text-white/40 bg-white/5 border-white/10';

  return (
    <div className="flex items-center justify-between rounded-xl border border-white/5 bg-white/[0.02] px-4 py-3">
      <div className="flex items-center gap-3 min-w-0">
        <span className="text-sm font-medium text-white/60 truncate">
          {booking.guest_name}
        </span>
        <span className="text-xs text-white/25">
          {tableLabel} · {booking.party_size} guests
        </span>
      </div>
      <div className="flex items-center gap-3">
        <span className="text-xs text-white/25">
          {getTimeSince(booking.created_at)}
        </span>
        <span className={`rounded-full border px-2 py-0.5 text-xs font-medium capitalize ${statusColor}`}>
          {booking.status}
        </span>
      </div>
    </div>
  );
}

// ── Helpers ───────────────────────────────────────────────────────

function getTimeSince(isoDate: string): string {
  const seconds = Math.floor((Date.now() - new Date(isoDate).getTime()) / 1000);
  if (seconds < 60) return 'just now';
  const minutes = Math.floor(seconds / 60);
  if (minutes < 60) return `${minutes}m ago`;
  const hours = Math.floor(minutes / 60);
  return `${hours}h ago`;
}
</file>

<file path="src/app/api/admin/verify/route.ts">
import { NextResponse } from 'next/server';

/**
 * Admin PIN verification endpoint.
 *
 * This is demo-only security, not production-grade.
 * In production, replace with a proper auth system (e.g. Supabase Auth, NextAuth).
 */
export async function POST(request: Request) {
  try {
    const { pin } = await request.json();
    const adminPin = process.env.ADMIN_PIN;

    if (!adminPin) {
      return NextResponse.json(
        { error: 'ADMIN_PIN not configured on server' },
        { status: 500 }
      );
    }

    if (pin === adminPin) {
      return NextResponse.json({ success: true });
    }

    return NextResponse.json(
      { error: 'Invalid PIN' },
      { status: 401 }
    );
  } catch {
    return NextResponse.json(
      { error: 'Invalid request' },
      { status: 400 }
    );
  }
}
</file>

<file path="src/app/globals.css">
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Inter:wght@300;400;500;600;700&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --gold: #D4AF37;
  --bg-dark: #0F0F12;
  --bg-card: #1a1a22;
}

* {
  -webkit-tap-highlight-color: transparent;
}

body {
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
  background-color: var(--bg-dark);
  color: white;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Serif headings */
.font-serif {
  font-family: 'Playfair Display', Georgia, serif;
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 3px;
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.2);
}

/* Modal slide-up animation */
@keyframes slide-up {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.animate-slide-up {
  animation: slide-up 0.35s cubic-bezier(0.16, 1, 0.3, 1);
}

/* Status pulse animation (gentler than default) */
@keyframes soft-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* Selection styling */
::selection {
  background: rgba(212, 175, 55, 0.3);
  color: white;
}
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "SkyDeck — Live Table Availability",
  description:
    "Book a table at SkyDeck Rooftop Restaurant, Pimpri-Chinchwad, Pune. See live availability across Rooftop, Indoor AC, and Outdoor zones.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-[#0F0F12] antialiased">
        {children}
      </body>
    </html>
  );
}
</file>

<file path="src/app/page.tsx">
'use client';

import { useState } from 'react';
import { MapPin } from 'lucide-react';
import { useRealtimeTables } from '@/hooks/useRealtimeTables';
import { useAutoExpiry } from '@/hooks/useAutoExpiry';
import { ZONES } from '@/lib/types';
import type { Table, Zone } from '@/lib/types';
import TableCard from '@/components/TableCard';
import BookingModal from '@/components/BookingModal';

export default function CustomerPage() {
  const { tables, loading } = useRealtimeTables();
  const [activeZone, setActiveZone] = useState<Zone>('Rooftop');
  const [selectedTable, setSelectedTable] = useState<Table | null>(null);

  // Run auto-expiry checker
  useAutoExpiry();

  const filteredTables = tables.filter((t) => t.zone === activeZone);

  return (
    <div className="min-h-screen bg-[#0F0F12]">
      {/* Warm gradient glow effect */}
      <div className="pointer-events-none fixed inset-0 bg-gradient-to-b from-amber-900/8 via-transparent to-transparent" />
      <div className="pointer-events-none fixed top-0 left-1/2 -translate-x-1/2 h-[500px] w-[800px] bg-amber-500/5 blur-[120px] rounded-full" />

      <div className="relative mx-auto max-w-2xl px-4 pb-8">
        {/* Header */}
        <header className="pb-6 pt-8 text-center">
          <h1 className="font-serif text-4xl font-bold tracking-tight text-white sm:text-5xl">
            Sky<span className="text-amber-400">Deck</span>
          </h1>
          <p className="mt-2 flex items-center justify-center gap-1.5 text-sm text-white/40">
            <MapPin size={14} className="text-amber-400/60" />
            Live Table Availability
          </p>
        </header>

        {/* Zone Tabs */}
        <div className="mb-6 flex gap-2 rounded-2xl border border-white/5 bg-white/[0.03] p-1.5 backdrop-blur-sm">
          {ZONES.map((zone) => {
            const count = tables.filter(
              (t) => t.zone === zone && t.status === 'available'
            ).length;
            const total = tables.filter((t) => t.zone === zone).length;

            return (
              <button
                key={zone}
                onClick={() => setActiveZone(zone)}
                className={`flex-1 rounded-xl px-3 py-2.5 text-sm font-medium transition-all duration-200 ${
                  activeZone === zone
                    ? 'bg-white/10 text-white shadow-sm'
                    : 'text-white/40 hover:text-white/60'
                }`}
              >
                <span className="block">{zone}</span>
                <span className={`text-xs ${
                  activeZone === zone ? 'text-emerald-400' : 'text-white/25'
                }`}>
                  {count}/{total} free
                </span>
              </button>
            );
          })}
        </div>

        {/* Loading state */}
        {loading && (
          <div className="flex items-center justify-center py-20">
            <div className="h-8 w-8 animate-spin rounded-full border-4 border-amber-500/30 border-t-amber-500" />
          </div>
        )}

        {/* Table Grid */}
        {!loading && (
          <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">
            {filteredTables.map((table) => (
              <TableCard
                key={table.id}
                table={table}
                onClick={() => {
                  if (table.status === 'available') {
                    setSelectedTable(table);
                  }
                }}
              />
            ))}
          </div>
        )}

        {/* Empty state */}
        {!loading && filteredTables.length === 0 && (
          <div className="py-20 text-center text-white/30">
            <p>No tables in this zone</p>
          </div>
        )}

        {/* Footer */}
        <footer className="mt-12 text-center text-xs text-white/20">
          <p>Rooftop Restaurant · Pimpri-Chinchwad, Pune</p>
        </footer>
      </div>

      {/* Booking Modal */}
      {selectedTable && (
        <BookingModal
          table={selectedTable}
          onClose={() => setSelectedTable(null)}
          onBooked={() => {
            // Modal handles its own confirmation state
          }}
        />
      )}
    </div>
  );
}
</file>

<file path="src/components/BookingModal.tsx">
'use client';

import { useState, useEffect, useCallback } from 'react';
import { X, Minus, Plus, CheckCircle2, AlertCircle } from 'lucide-react';
import { supabase } from '@/lib/supabase';
import type { Table } from '@/lib/types';
import { EXPIRY_MINUTES } from '@/lib/types';

interface BookingModalProps {
  table: Table;
  onClose: () => void;
  onBooked: () => void;
}

type ModalState = 'form' | 'submitting' | 'confirmed' | 'error';

export default function BookingModal({ table, onClose, onBooked }: BookingModalProps) {
  const [state, setState] = useState<ModalState>('form');
  const [name, setName] = useState('');
  const [phone, setPhone] = useState('');
  const [partySize, setPartySize] = useState(1);
  const [note, setNote] = useState('');
  const [errorMsg, setErrorMsg] = useState('');
  const [bookingRef, setBookingRef] = useState('');
  const [countdown, setCountdown] = useState(EXPIRY_MINUTES * 60);
  const [bookingCreatedAt, setBookingCreatedAt] = useState<Date | null>(null);

  // Phone validation: exactly 10 digits (India format)
  const isPhoneValid = /^\d{10}$/.test(phone);
  const isFormValid = name.trim().length > 0 && isPhoneValid && partySize >= 1;

  // Countdown timer
  useEffect(() => {
    if (state !== 'confirmed' || !bookingCreatedAt) return;

    const interval = setInterval(() => {
      const elapsed = Math.floor((Date.now() - bookingCreatedAt.getTime()) / 1000);
      const remaining = EXPIRY_MINUTES * 60 - elapsed;
      setCountdown(Math.max(0, remaining));
      if (remaining <= 0) {
        clearInterval(interval);
      }
    }, 1000);

    return () => clearInterval(interval);
  }, [state, bookingCreatedAt]);

  const handleSubmit = useCallback(async () => {
    if (!isFormValid) return;

    setState('submitting');

    try {
      // Race condition guard: re-check table is still available
      const { data: freshTable, error: fetchError } = await supabase
        .from('tables')
        .select('status')
        .eq('id', table.id)
        .single();

      if (fetchError || !freshTable) {
        setErrorMsg('Unable to verify table status. Please try again.');
        setState('error');
        return;
      }

      if (freshTable.status !== 'available') {
        setErrorMsg('Sorry, this table was just taken — please pick another.');
        setState('error');
        return;
      }

      // Insert booking
      const { data: booking, error: bookingError } = await supabase
        .from('bookings')
        .insert({
          table_id: table.id,
          guest_name: name.trim(),
          phone: phone.trim(),
          party_size: partySize,
          note: note.trim() || null,
          status: 'pending',
        })
        .select()
        .single();

      if (bookingError || !booking) {
        setErrorMsg('Failed to create booking. Please try again.');
        setState('error');
        return;
      }

      // Update table to pending
      await supabase
        .from('tables')
        .update({ status: 'pending', updated_at: new Date().toISOString() })
        .eq('id', table.id);

      // Show confirmation
      setBookingRef(booking.id.slice(-6).toUpperCase());
      setBookingCreatedAt(new Date(booking.created_at));
      setState('confirmed');
      onBooked();
    } catch {
      setErrorMsg('Something went wrong. Please try again.');
      setState('error');
    }
  }, [isFormValid, table.id, name, phone, partySize, note, onBooked]);

  const formatTime = (seconds: number) => {
    const m = Math.floor(seconds / 60);
    const s = seconds % 60;
    return `${m}:${s.toString().padStart(2, '0')}`;
  };

  return (
    <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center">
      {/* Backdrop */}
      <div
        className="absolute inset-0 bg-black/60 backdrop-blur-sm"
        onClick={onClose}
      />

      {/* Modal panel */}
      <div className="relative z-10 w-full max-w-md rounded-t-3xl sm:rounded-3xl border border-white/10 bg-[#1a1a22]/95 p-6 shadow-2xl backdrop-blur-md animate-slide-up">
        {/* Close button */}
        <button
          onClick={onClose}
          className="absolute right-4 top-4 rounded-full p-1.5 text-white/40 transition-colors hover:bg-white/10 hover:text-white"
        >
          <X size={20} />
        </button>

        {state === 'form' && (
          <>
            <h2 className="font-serif text-2xl font-bold text-white mb-1">
              Book Table {table.label}
            </h2>
            <p className="text-sm text-white/50 mb-6">
              {table.zone} · {table.capacity} seats
            </p>

            {/* Name */}
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-white/70">
                Name <span className="text-amber-400">*</span>
              </label>
              <input
                type="text"
                value={name}
                onChange={(e) => setName(e.target.value)}
                placeholder="Your name"
                className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-white/30 focus:border-amber-500/50 focus:outline-none focus:ring-1 focus:ring-amber-500/30 transition-colors"
              />
            </div>

            {/* Phone */}
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-white/70">
                Phone <span className="text-amber-400">*</span>
              </label>
              <div className="flex items-center gap-2">
                <span className="rounded-xl border border-white/10 bg-white/5 px-3 py-3 text-sm text-white/50">
                  +91
                </span>
                <input
                  type="tel"
                  value={phone}
                  onChange={(e) => setPhone(e.target.value.replace(/\D/g, '').slice(0, 10))}
                  placeholder="10-digit number"
                  className="flex-1 rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-white/30 focus:border-amber-500/50 focus:outline-none focus:ring-1 focus:ring-amber-500/30 transition-colors"
                />
              </div>
              {phone.length > 0 && !isPhoneValid && (
                <p className="mt-1 text-xs text-red-400">
                  Please enter a valid 10-digit phone number
                </p>
              )}
            </div>

            {/* Party Size Stepper */}
            <div className="mb-4">
              <label className="mb-1.5 block text-sm font-medium text-white/70">
                Party Size
              </label>
              <div className="flex items-center gap-3">
                <button
                  onClick={() => setPartySize((s) => Math.max(1, s - 1))}
                  disabled={partySize <= 1}
                  className="rounded-xl border border-white/10 bg-white/5 p-2.5 text-white transition-colors hover:bg-white/10 disabled:opacity-30 disabled:cursor-not-allowed"
                >
                  <Minus size={18} />
                </button>
                <span className="min-w-[3rem] text-center text-xl font-bold text-white">
                  {partySize}
                </span>
                <button
                  onClick={() =>
                    setPartySize((s) => Math.min(table.capacity, s + 1))
                  }
                  disabled={partySize >= table.capacity}
                  className="rounded-xl border border-white/10 bg-white/5 p-2.5 text-white transition-colors hover:bg-white/10 disabled:opacity-30 disabled:cursor-not-allowed"
                >
                  <Plus size={18} />
                </button>
              </div>
              {partySize >= table.capacity && (
                <p className="mt-1.5 text-xs text-amber-400/80">
                  For larger groups, please contact the desk.
                </p>
              )}
            </div>

            {/* Note */}
            <div className="mb-6">
              <label className="mb-1.5 block text-sm font-medium text-white/70">
                Note <span className="text-white/30">(optional)</span>
              </label>
              <input
                type="text"
                value={note}
                onChange={(e) => setNote(e.target.value)}
                placeholder='e.g. "birthday", "near the edge"'
                className="w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-white/30 focus:border-amber-500/50 focus:outline-none focus:ring-1 focus:ring-amber-500/30 transition-colors"
              />
            </div>

            {/* Submit */}
            <button
              onClick={handleSubmit}
              disabled={!isFormValid}
              className="w-full rounded-xl bg-gradient-to-r from-amber-600 to-amber-500 px-6 py-3.5 font-semibold text-white shadow-lg shadow-amber-500/20 transition-all hover:shadow-amber-500/40 disabled:opacity-40 disabled:cursor-not-allowed disabled:shadow-none active:scale-[0.98]"
            >
              Request This Table
            </button>
          </>
        )}

        {state === 'submitting' && (
          <div className="flex flex-col items-center py-12">
            <div className="h-10 w-10 animate-spin rounded-full border-4 border-amber-500/30 border-t-amber-500" />
            <p className="mt-4 text-white/60">Reserving your table…</p>
          </div>
        )}

        {state === 'confirmed' && (
          <div className="flex flex-col items-center py-6 text-center">
            <div className="mb-4 rounded-full bg-emerald-500/10 p-3">
              <CheckCircle2 size={40} className="text-emerald-400" />
            </div>
            <h2 className="font-serif text-2xl font-bold text-white mb-2">
              Table Requested!
            </h2>
            <p className="text-sm text-white/50 mb-4">
              Booking Ref: <span className="font-mono text-amber-400 font-bold">{bookingRef}</span>
            </p>
            <p className="text-sm text-white/60 leading-relaxed mb-6 max-w-sm">
              Please walk up to the reception desk to confirm your table within 10 minutes, or it will be released automatically.
            </p>

            {/* Countdown */}
            <div className="rounded-2xl border border-white/10 bg-white/5 px-6 py-4 backdrop-blur-sm">
              <p className="text-xs text-white/40 uppercase tracking-wider mb-1">
                Time Remaining
              </p>
              <p className={`font-mono text-3xl font-bold ${
                countdown <= 60 ? 'text-red-400' : countdown <= 180 ? 'text-amber-400' : 'text-white'
              }`}>
                {formatTime(countdown)}
              </p>
            </div>
          </div>
        )}

        {state === 'error' && (
          <div className="flex flex-col items-center py-6 text-center">
            <div className="mb-4 rounded-full bg-red-500/10 p-3">
              <AlertCircle size={40} className="text-red-400" />
            </div>
            <h2 className="font-serif text-xl font-bold text-white mb-2">
              Oops!
            </h2>
            <p className="text-sm text-white/60 mb-6">{errorMsg}</p>
            <button
              onClick={onClose}
              className="rounded-xl border border-white/10 bg-white/5 px-6 py-2.5 text-sm font-medium text-white transition-colors hover:bg-white/10"
            >
              Pick Another Table
            </button>
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/components/TableCard.tsx">
'use client';

import { Users } from 'lucide-react';
import type { Table, TableStatus } from '@/lib/types';
import { STATUS_CONFIG } from '@/lib/types';

interface TableCardProps {
  table: Table;
  onClick?: () => void;
  /** If true, card is always clickable (admin mode) */
  adminMode?: boolean;
}

export default function TableCard({ table, onClick, adminMode }: TableCardProps) {
  const config = STATUS_CONFIG[table.status as TableStatus];
  const isClickable = adminMode || table.status === 'available';

  return (
    <button
      type="button"
      onClick={isClickable ? onClick : undefined}
      disabled={!isClickable}
      className={`
        group relative w-full rounded-2xl border p-4 text-left
        transition-all duration-300 ease-out
        ${config.border} ${config.bg}
        ${isClickable
          ? 'cursor-pointer hover:scale-[1.03] hover:shadow-lg active:scale-[0.98]'
          : 'cursor-default opacity-80'
        }
        ${config.glow}
        backdrop-blur-sm
      `}
    >
      {/* Status indicator line */}
      <div
        className={`absolute left-0 top-0 h-full w-1 rounded-l-2xl transition-colors duration-300 ${
          table.status === 'available'
            ? 'bg-emerald-500'
            : table.status === 'pending'
            ? 'bg-amber-500'
            : table.status === 'occupied'
            ? 'bg-red-500'
            : 'bg-sky-500'
        }`}
      />

      {/* Table label */}
      <div className="mb-2 flex items-center justify-between">
        <span className="font-serif text-xl font-bold text-white/90">
          {table.label}
        </span>
        {isClickable && (
          <span className="text-xs text-white/30 opacity-0 transition-opacity group-hover:opacity-100">
            {adminMode ? 'Tap to cycle' : 'Tap to book'}
          </span>
        )}
      </div>

      {/* Capacity */}
      <div className="mb-3 flex items-center gap-1.5 text-sm text-white/50">
        <Users size={14} />
        <span>{table.capacity} seats</span>
      </div>

      {/* Status badge */}
      <div
        className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium ${config.bg} ${config.color} ${config.border} border`}
      >
        <span
          className={`h-1.5 w-1.5 rounded-full ${
            table.status === 'available'
              ? 'bg-emerald-400 animate-pulse'
              : table.status === 'pending'
              ? 'bg-amber-400 animate-pulse'
              : table.status === 'occupied'
              ? 'bg-red-400'
              : 'bg-sky-400 animate-pulse'
          }`}
        />
        {config.label}
      </div>
    </button>
  );
}
</file>

<file path="src/hooks/useAutoExpiry.ts">
'use client';

import { useEffect, useRef } from 'react';
import { supabase } from '@/lib/supabase';
import { EXPIRY_MINUTES } from '@/lib/types';

/**
 * Client-side auto-expiry for pending bookings.
 *
 * Every 30 seconds, checks for pending bookings older than EXPIRY_MINUTES
 * and marks them as expired, returning their table to 'available'.
 *
 * NOTE: In production, this should be replaced with a Supabase Edge Function
 * or a pg_cron job to ensure expiry happens even when no client is connected.
 */
export function useAutoExpiry() {
  const running = useRef(false);

  useEffect(() => {
    const checkExpiry = async () => {
      if (running.current) return;
      running.current = true;

      try {
        const cutoff = new Date(
          Date.now() - EXPIRY_MINUTES * 60 * 1000
        ).toISOString();

        // Find pending bookings that are past the expiry window
        const { data: expiredBookings, error } = await supabase
          .from('bookings')
          .select('id, table_id')
          .eq('status', 'pending')
          .lt('created_at', cutoff);

        if (error || !expiredBookings || expiredBookings.length === 0) {
          return;
        }

        // Expire each booking and release its table
        for (const booking of expiredBookings) {
          await supabase
            .from('bookings')
            .update({ status: 'expired' })
            .eq('id', booking.id);

          await supabase
            .from('tables')
            .update({ status: 'available', updated_at: new Date().toISOString() })
            .eq('id', booking.table_id);
        }
      } catch (err) {
        console.error('Auto-expiry error:', err);
      } finally {
        running.current = false;
      }
    };

    // Run immediately on mount, then every 30s
    checkExpiry();
    const interval = setInterval(checkExpiry, 30_000);

    return () => clearInterval(interval);
  }, []);
}
</file>

<file path="src/hooks/useRealtimeBookings.ts">
'use client';

import { useEffect, useState, useCallback } from 'react';
import { supabase } from '@/lib/supabase';
import type { Booking } from '@/lib/types';

/**
 * Subscribe to the `bookings` table via Supabase Realtime.
 * Optionally filter by booking status(es).
 */
export function useRealtimeBookings(statusFilter?: string[]) {
  const [bookings, setBookings] = useState<Booking[]>([]);
  const [loading, setLoading] = useState(true);

  const fetchBookings = useCallback(async () => {
    let query = supabase
      .from('bookings')
      .select('*, tables(*)')
      .order('created_at', { ascending: false });

    if (statusFilter && statusFilter.length > 0) {
      query = query.in('status', statusFilter);
    }

    const { data, error } = await query;
    if (error) {
      console.error('Error fetching bookings:', error);
      return;
    }
    setBookings(data as Booking[]);
    setLoading(false);
  }, [statusFilter]);

  useEffect(() => {
    fetchBookings();

    const channel = supabase
      .channel('bookings-realtime')
      .on(
        'postgres_changes',
        { event: '*', schema: 'public', table: 'bookings' },
        () => {
          // Re-fetch on any change to get the joined table data
          fetchBookings();
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }, [fetchBookings]);

  return { bookings, loading, refetch: fetchBookings };
}
</file>

<file path="src/hooks/useRealtimeTables.ts">
'use client';

import { useEffect, useState, useCallback } from 'react';
import { supabase } from '@/lib/supabase';
import type { Table } from '@/lib/types';

/**
 * Subscribe to the `tables` table via Supabase Realtime.
 * Returns the live list of tables, grouped by zone.
 */
export function useRealtimeTables() {
  const [tables, setTables] = useState<Table[]>([]);
  const [loading, setLoading] = useState(true);

  const fetchTables = useCallback(async () => {
    const { data, error } = await supabase
      .from('tables')
      .select('*')
      .order('label', { ascending: true });

    if (error) {
      console.error('Error fetching tables:', error);
      return;
    }
    setTables(data as Table[]);
    setLoading(false);
  }, []);

  useEffect(() => {
    fetchTables();

    const channel = supabase
      .channel('tables-realtime')
      .on(
        'postgres_changes',
        { event: '*', schema: 'public', table: 'tables' },
        (payload) => {
          setTables((prev) => {
            if (payload.eventType === 'INSERT') {
              return [...prev, payload.new as Table];
            }
            if (payload.eventType === 'UPDATE') {
              return prev.map((t) =>
                t.id === (payload.new as Table).id ? (payload.new as Table) : t
              );
            }
            if (payload.eventType === 'DELETE') {
              return prev.filter((t) => t.id !== (payload.old as Table).id);
            }
            return prev;
          });
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }, [fetchTables]);

  return { tables, loading, refetch: fetchTables };
}
</file>

<file path="src/lib/supabase.ts">
import { createClient } from '@supabase/supabase-js';

/**
 * Supabase browser client.
 *
 * NOTE: RLS is intentionally disabled / fully permissive for this demo.
 * The anon key can select, insert, and update on both `tables` and `bookings`.
 * This MUST be locked down before any production use.
 */
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
</file>

<file path="src/lib/types.ts">
// ---- Enums (matching Postgres enums) ----

export type TableStatus = 'available' | 'pending' | 'occupied' | 'cleaning';
export type BookingStatus = 'pending' | 'confirmed' | 'cancelled' | 'expired';

// ---- Row types ----

export interface Table {
  id: string;
  label: string;
  zone: string;       // 'Rooftop' | 'Indoor AC' | 'Outdoor'
  capacity: number;
  status: TableStatus;
  updated_at: string;  // ISO-8601
}

export interface Booking {
  id: string;
  table_id: string;
  guest_name: string;
  phone: string;
  party_size: number;
  note: string | null;
  status: BookingStatus;
  created_at: string;  // ISO-8601

  // Joined field (optional — populated by select queries with join)
  tables?: Table;
}

// ---- Constants ----

export const ZONES = ['Rooftop', 'Indoor AC', 'Outdoor'] as const;
export type Zone = (typeof ZONES)[number];

/** Pending bookings auto-expire after this many minutes. */
export const EXPIRY_MINUTES = 10;

/** Status display config */
export const STATUS_CONFIG: Record<
  TableStatus,
  { label: string; color: string; glow: string; bg: string; border: string }
> = {
  available: {
    label: 'Available',
    color: 'text-emerald-400',
    glow: 'shadow-emerald-500/20',
    bg: 'bg-emerald-500/10',
    border: 'border-emerald-500/40',
  },
  pending: {
    label: 'Reserved — Confirming',
    color: 'text-amber-400',
    glow: 'shadow-amber-500/20',
    bg: 'bg-amber-500/10',
    border: 'border-amber-500/40',
  },
  occupied: {
    label: 'Occupied',
    color: 'text-red-400',
    glow: 'shadow-red-500/20',
    bg: 'bg-red-500/10',
    border: 'border-red-500/40',
  },
  cleaning: {
    label: 'Being Cleaned',
    color: 'text-sky-400',
    glow: 'shadow-sky-500/20',
    bg: 'bg-sky-500/10',
    border: 'border-sky-500/40',
  },
};
</file>

<file path=".eslintrc.json">
{
  "extends": ["next/core-web-vitals", "next/typescript"]
}
</file>

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
</file>

<file path="next.config.mjs">
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
</file>

<file path="package.json">
{
  "name": "skydeck-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "lucide-react": "^1.31.0",
    "next": "14.2.35",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.35",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
</file>

<file path="postcss.config.mjs">
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
</file>

<file path="PROJECT_GUIDE.txt">
================================================================================
                    SKYDECK TABLE BOOKING DEMO — PROJECT GUIDE
================================================================================

  A real-time table-status and booking system for SkyDeck Rooftop Restaurant
  Pimpri-Chinchwad, Pune

================================================================================


TABLE OF CONTENTS
-----------------
  1. Project Overview
  2. Tech Stack
  3. Folder Structure
  4. Environment Variables
  5. Database Schema
  6. Seed Data
  7. Pages & Navigation
     7.1  Customer Page (/)
     7.2  Admin Page (/admin)
     7.3  API Endpoint (/api/admin/verify)
  8. Real-Time Architecture
  9. Key Features
  10. How to Run Locally
  11. Deployment (Vercel)
  12. Acceptance Criteria / Demo Script
  13. What's Out of Scope
  14. Production Notes


================================================================================
1. PROJECT OVERVIEW
================================================================================

SkyDeck is a no-login, real-time table booking demo built for a rooftop
restaurant. It has exactly two user-facing pages:

  • A CUSTOMER PAGE (/) — meant to be opened after scanning a QR code on
    the restaurant's menu or table tent. Shows live table availability
    across three zones and lets guests request a table.

  • An ADMIN PAGE (/admin) — used by staff on a tablet or desktop. Shows
    live incoming booking requests, lets staff confirm/release them, and
    allows manual table status management (e.g. marking a table as
    occupied after a walk-in, or as "cleaning" after a guest leaves).

Both pages share the same live data through Supabase Realtime. A status
change on one page is reflected on the other within 2–3 seconds, with no
manual refresh needed.


================================================================================
2. TECH STACK
================================================================================

  Framework       :  Next.js 14 (App Router) + TypeScript
  Styling         :  Tailwind CSS
  Database        :  Supabase (PostgreSQL) — free tier
  Real-Time Sync  :  Supabase Realtime (Postgres Changes)
  Icons           :  lucide-react
  Fonts           :  Playfair Display (serif headings) + Inter (body text)
                     loaded via Google Fonts
  Deploy Target   :  Vercel
  Package Manager :  npm

  NO other backend, no auth provider, no payment SDK, no SMS/WhatsApp.


================================================================================
3. FOLDER STRUCTURE
================================================================================

  Sky/
  ├── .env.local                          # Environment variables (secrets)
  ├── seed.sql                            # SQL to seed 16 tables (idempotent)
  ├── package.json                        # Node dependencies & scripts
  ├── tailwind.config.ts                  # Tailwind theme (gold palette, fonts)
  ├── tsconfig.json                       # TypeScript configuration
  ├── next.config.mjs                     # Next.js configuration
  ├── postcss.config.mjs                  # PostCSS (required by Tailwind)
  │
  └── src/
      ├── lib/
      │   ├── supabase.ts                 # Supabase client singleton
      │   └── types.ts                    # TypeScript types, enums, constants
      │
      ├── hooks/
      │   ├── useRealtimeTables.ts        # Live subscription to `tables` table
      │   ├── useRealtimeBookings.ts      # Live subscription to `bookings` table
      │   └── useAutoExpiry.ts            # Auto-expires stale pending bookings
      │
      ├── components/
      │   ├── TableCard.tsx               # Individual table card (status colors)
      │   └── BookingModal.tsx            # Booking form + confirmation + countdown
      │
      └── app/
          ├── layout.tsx                  # Root HTML layout + SEO metadata
          ├── globals.css                 # Global styles, fonts, animations
          ├── page.tsx                    # Customer page (/)
          │
          ├── admin/
          │   └── page.tsx                # Admin dashboard (/admin)
          │
          └── api/
              └── admin/
                  └── verify/
                      └── route.ts        # PIN verification API endpoint


================================================================================
4. ENVIRONMENT VARIABLES
================================================================================

  These are stored in .env.local (never committed to Git):

  NEXT_PUBLIC_SUPABASE_URL       Your Supabase project URL
                                 (e.g. https://xxxxx.supabase.co)

  NEXT_PUBLIC_SUPABASE_ANON_KEY  Your Supabase anonymous/public API key
                                 (safe for the browser — gated by RLS)

  ADMIN_PIN                      A numeric PIN for admin page access
                                 (server-side only — never exposed to client)

  The NEXT_PUBLIC_ prefix means those two values are bundled into the
  client-side JavaScript. The ADMIN_PIN is only read server-side in the
  /api/admin/verify route.


================================================================================
5. DATABASE SCHEMA
================================================================================

  The database lives in Supabase (PostgreSQL). Two custom enums and two
  tables are used:

  ENUMS:
  ------
    table_status   :  'available' | 'pending' | 'occupied' | 'cleaning'
    booking_status :  'pending' | 'confirmed' | 'cancelled' | 'expired'

  TABLE: tables
  -------------
    id          UUID (primary key, auto-generated)
    label       TEXT (e.g. "R4", "IN2", "O3")
    zone        TEXT ('Rooftop' | 'Indoor AC' | 'Outdoor')
    capacity    INT  (number of seats)
    status      table_status (default: 'available')
    updated_at  TIMESTAMPTZ (auto-set)

  TABLE: bookings
  ---------------
    id          UUID (primary key, auto-generated)
    table_id    UUID (foreign key → tables.id)
    guest_name  TEXT
    phone       TEXT (10-digit Indian number)
    party_size  INT
    note        TEXT (nullable)
    status      booking_status (default: 'pending')
    created_at  TIMESTAMPTZ (auto-set)

  IMPORTANT: Supabase Realtime must be enabled on BOTH tables
  (Database → Replication → enable tables & bookings).

  Row Level Security (RLS) is set to fully permissive for this demo.
  This must be locked down before any real production use.


================================================================================
6. SEED DATA
================================================================================

  The seed.sql file inserts 16 tables across three zones. It is
  idempotent (uses ON CONFLICT DO NOTHING), so it's safe to run
  multiple times.

  ROOFTOP (R1–R6):
    R1 = 2 seats,  R2 = 4 seats,  R3 = 4 seats
    R4 = 6 seats,  R5 = 4 seats,  R6 = 2 seats

  INDOOR AC (IN1–IN5):
    IN1 = 2 seats, IN2 = 4 seats, IN3 = 4 seats
    IN4 = 6 seats, IN5 = 8 seats

  OUTDOOR (O1–O5):
    O1 = 2 seats,  O2 = 4 seats,  O3 = 4 seats
    O4 = 6 seats,  O5 = 4 seats

  NOTE: The banquet hall is intentionally excluded from this demo.
  It is normally booked as a whole event space, not walk-in tables.

  HOW TO RUN: Copy the contents of seed.sql into the Supabase
  Dashboard → SQL Editor → New Query → Run.


================================================================================
7. PAGES & NAVIGATION
================================================================================

  There are exactly two user-facing routes and one API route:

    /               → Customer Page (public, no login)
    /admin          → Admin Page (PIN-protected)
    /api/admin/verify → PIN verification endpoint (internal)


  7.1  CUSTOMER PAGE  (/)
  ========================

    PURPOSE:
      Scanned via QR code by restaurant guests. Shows live table
      availability and lets guests request a table.

    LAYOUT:
      • Header: "SkyDeck" wordmark (gold accent) + "Live Table Availability"
      • Zone tabs: Rooftop / Indoor AC / Outdoor
        Each tab shows "X/Y free" count
      • Table grid: 2-column on mobile, 3-column on desktop
      • Footer: Restaurant name and location

    TABLE CARDS:
      Each card shows the table label (e.g. "R4"), seat capacity
      with a people icon, and a color-coded status badge:

        Available  → Green badge, pulsing dot, card is tappable
        Pending    → Amber badge, text "Reserved — Confirming"
        Occupied   → Red badge
        Cleaning   → Blue/sky badge, text "Being Cleaned"

      Only "Available" tables can be tapped. Others are view-only.

    BOOKING FLOW:
      1. Tap an available table → a modal/bottom-sheet opens
      2. Fill in:
         • Name (required)
         • Phone (required, 10-digit Indian number with +91 prefix)
         • Party size (stepper: min 1, max = table capacity)
         • Note (optional, e.g. "birthday", "near the edge")
      3. Tap "Request This Table"
      4. The system re-checks the table is still available (race guard)
         • If taken: shows "Sorry, this table was just taken" error
         • If available: creates the booking and updates table to pending
      5. Confirmation screen appears:
         • Booking reference (last 6 characters of booking ID)
         • Message: "Please walk up to reception within 10 minutes"
         • Live countdown timer (10:00 → 0:00)

    AUTO-EXPIRY:
      A background interval runs every 30 seconds. It checks for
      pending bookings older than 10 minutes and automatically:
        • Sets the booking status to "expired"
        • Returns the table status to "available"

      NOTE: In production, this should be a Supabase Edge Function
      or pg_cron job, not a client-side timer.


  7.2  ADMIN PAGE  (/admin)
  ==========================

    ACCESS:
      A PIN entry screen gates access. The PIN is validated server-side
      against the ADMIN_PIN environment variable. Once validated, the
      session is stored in the browser's sessionStorage (tab-scoped).

      This is demo-only security, not production-grade.

    LAYOUT (after PIN entry):

      HEADER:
        "SkyDeck Staff Panel" with a "Sign Out" button

      SECTION 1 — LIVE TABLE GRID:
        Same visual grid as the customer page, but ALL tables are
        clickable regardless of status. Clicking cycles the status:

          Available → Occupied → Cleaning → Available

        This lets staff quickly mark walk-ins (→ occupied) or
        finished tables (→ cleaning → available) without going
        through the booking flow.

      SECTION 2 — REQUEST QUEUE:
        Shows all pending bookings, newest first. Each card shows:
          • Guest name
          • Phone number (tap-to-call link with +91 prefix)
          • Party size
          • Table label (e.g. "R4")
          • Optional note
          • Time since request (e.g. "3m ago")
          • Live countdown to expiry

        Two action buttons per booking:
          • "Confirm & Seat" → booking → confirmed, table → occupied
          • "Release" → booking → cancelled, table → available

      SECTION 3 — RECENT HISTORY (collapsed by default):
        Shows the last 10 confirmed/cancelled/expired bookings
        for staff context. Visually secondary. Click to expand.

    Everything updates live via Supabase Realtime — no refresh needed.


  7.3  API ENDPOINT  (/api/admin/verify)
  ========================================

    METHOD:  POST
    BODY:    { "pin": "123456" }
    PURPOSE: Validates the PIN against process.env.ADMIN_PIN
    RETURNS: { "success": true } or { "error": "Invalid PIN" }

    This keeps the ADMIN_PIN server-side only. The client never
    sees the actual PIN value.


================================================================================
8. REAL-TIME ARCHITECTURE
================================================================================

  The app uses Supabase Realtime's "Postgres Changes" feature to
  keep both pages in sync:

    ┌─────────────────┐         ┌──────────────┐
    │  Customer Page   │◄───────►│   Supabase   │
    │  (phone/browser) │  WS     │  Realtime    │
    └─────────────────┘         │  (WebSocket) │
                                │              │
    ┌─────────────────┐         │              │
    │  Admin Page      │◄───────►│              │
    │  (tablet/laptop) │  WS     └──────┬───────┘
    └─────────────────┘                │
                                ┌──────┴───────┐
                                │  PostgreSQL  │
                                │  (tables +   │
                                │   bookings)  │
                                └──────────────┘

  FLOW:
    1. Customer or admin writes to Postgres (insert/update)
    2. Supabase Realtime broadcasts the change via WebSocket
    3. All connected clients receive the event and update their UI

  TWO CHANNELS:
    • "tables-realtime" — listens for INSERT/UPDATE/DELETE on `tables`
    • "bookings-realtime" — listens for changes on `bookings`

  The hooks (useRealtimeTables.ts and useRealtimeBookings.ts) manage
  these subscriptions. On mount they fetch the initial data, then
  apply real-time patches as events arrive.


================================================================================
9. KEY FEATURES
================================================================================

  ✓ Live table grid with color-coded status
  ✓ Zone-based filtering (Rooftop / Indoor AC / Outdoor)
  ✓ Booking form with input validation (10-digit phone, party size cap)
  ✓ Race condition handling (double-booking prevention)
  ✓ Confirmation screen with booking reference and 10-min countdown
  ✓ Auto-expiry of unconfirmed bookings after 10 minutes
  ✓ Admin PIN gate (server-side validation)
  ✓ Admin table status cycling (available → occupied → cleaning)
  ✓ Admin request queue with Confirm & Seat / Release actions
  ✓ Tap-to-call phone links on admin booking cards
  ✓ Recent booking history (collapsible)
  ✓ Cross-device real-time sync via Supabase Realtime
  ✓ Mobile-first responsive design
  ✓ Dark "rooftop at night" aesthetic with gold accents
  ✓ Smooth CSS animations (modal slide-up, hover effects, pulse)
  ✓ SEO metadata (title, description)


================================================================================
10. HOW TO RUN LOCALLY
================================================================================

  PREREQUISITES:
    • Node.js 18+ installed
    • A Supabase project with the tables/enums already created
    • Realtime enabled on both tables in Supabase Dashboard

  STEPS:

    1. Clone or download the project

    2. Make sure .env.local exists with your values:
         NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
         NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
         ADMIN_PIN=your-chosen-pin

    3. Install dependencies:
         npm install

    4. Seed the database (if not already done):
         Copy seed.sql contents → Supabase Dashboard → SQL Editor → Run

    5. Start the development server:
         npm run dev

    6. Open in browser:
         Customer page:  http://localhost:3000
         Admin page:     http://localhost:3000/admin


================================================================================
11. DEPLOYMENT (VERCEL)
================================================================================

  1. Push the project to a GitHub repository

  2. Import the repo in Vercel (vercel.com → New Project)

  3. Set these environment variables in Vercel's project settings:
       NEXT_PUBLIC_SUPABASE_URL      = your Supabase URL
       NEXT_PUBLIC_SUPABASE_ANON_KEY = your Supabase anon key
       ADMIN_PIN                     = your admin PIN

  4. Deploy — Vercel auto-detects Next.js and builds it

  5. The customer page will be live at your Vercel URL's root (/)
     The admin page will be at /admin

  You can then attach a custom domain and generate a QR code
  pointing to the customer page.


================================================================================
12. ACCEPTANCE CRITERIA / DEMO SCRIPT
================================================================================

  These are the exact scenarios the app is designed to handle:

  1. Open / on a phone
     → See the 16-table grid grouped by zone, all green/available

  2. Tap an available table → fill form → submit
     → See confirmation + 10-min countdown
     → Table turns amber on the grid

  3. Open /admin on a laptop (enter PIN)
     → See the new request appear in the queue within seconds
     → No page refresh needed

  4. Click "Confirm & Seat" on admin
     → Table turns red/occupied on BOTH the admin grid AND the
       customer page (open in another tab) within seconds

  5. On admin grid, click an occupied table
     → Cycles to "Cleaning" → click again → "Available"
     → Reflected live on customer page

  6. Try booking an already-pending table from a second browser tab
     → Get the "Sorry, this table was just taken" message
     → No duplicate booking created

  7. Leave a booking unconfirmed for 10+ minutes
     → It auto-expires and the table returns to available


================================================================================
13. WHAT'S OUT OF SCOPE
================================================================================

  The following features are intentionally NOT built in this demo:

    ✗ Payments or deposits
    ✗ SMS / WhatsApp / email notifications
    ✗ Menu browsing or food ordering
    ✗ Multi-restaurant support
    ✗ Real staff accounts/roles (just a shared PIN)
    ✗ Banquet hall event booking
    ✗ Waitlist / queue-position algorithm


================================================================================
14. PRODUCTION NOTES
================================================================================

  If this demo were to go into real production use, these changes
  would be necessary:

  SECURITY:
    • Enable Row Level Security (RLS) on both tables with proper
      policies (e.g. anon can only SELECT tables, INSERT bookings)
    • Replace the shared PIN with Supabase Auth or NextAuth for
      real staff accounts and role-based access

  AUTO-EXPIRY:
    • Replace the client-side expiry timer with a Supabase Edge
      Function or pg_cron job that runs every minute server-side

  NOTIFICATIONS:
    • Add SMS/WhatsApp confirmation when a booking is confirmed
    • Alert staff via push notification for new requests

  SCALE:
    • Add proper error boundaries and loading states
    • Implement optimistic UI updates for snappier interactions
    • Add rate limiting on the booking endpoint
    • Consider connection pooling for high-traffic scenarios


================================================================================
                              END OF PROJECT GUIDE
================================================================================
</file>

<file path="README.md">
This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Learn More

To learn more about Next.js, take a look at the following resources:

- [Next.js Documentation](https://nextjs.org/docs) - learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) - an interactive Next.js tutorial.

You can check out [the Next.js GitHub repository](https://github.com/vercel/next.js) - your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.
</file>

<file path="seed.sql">
-- SkyDeck Table Booking Demo — Seed Data
--
-- Assumption: The banquet hall is intentionally excluded from this demo.
-- It is normally booked as a whole event space (not individual walk-in tables)
-- and will be handled separately in a future version.
--
-- This script is idempotent — it uses ON CONFLICT to avoid duplicating rows
-- if run multiple times. Conflict is checked on the label column (assumes unique).

-- Rooftop zone: R1–R6, capacities 2/4/4/6/4/2
INSERT INTO tables (label, zone, capacity, status) VALUES
  ('R1', 'Rooftop', 2, 'available'),
  ('R2', 'Rooftop', 4, 'available'),
  ('R3', 'Rooftop', 4, 'available'),
  ('R4', 'Rooftop', 6, 'available'),
  ('R5', 'Rooftop', 4, 'available'),
  ('R6', 'Rooftop', 2, 'available')
ON CONFLICT (label) DO NOTHING;

-- Indoor AC zone: IN1–IN5, capacities 2/4/4/6/8
INSERT INTO tables (label, zone, capacity, status) VALUES
  ('IN1', 'Indoor AC', 2, 'available'),
  ('IN2', 'Indoor AC', 4, 'available'),
  ('IN3', 'Indoor AC', 4, 'available'),
  ('IN4', 'Indoor AC', 6, 'available'),
  ('IN5', 'Indoor AC', 8, 'available')
ON CONFLICT (label) DO NOTHING;

-- Outdoor zone: O1–O5, capacities 2/4/4/6/4
INSERT INTO tables (label, zone, capacity, status) VALUES
  ('O1', 'Outdoor', 2, 'available'),
  ('O2', 'Outdoor', 4, 'available'),
  ('O3', 'Outdoor', 4, 'available'),
  ('O4', 'Outdoor', 6, 'available'),
  ('O5', 'Outdoor', 4, 'available')
ON CONFLICT (label) DO NOTHING;
</file>

<file path="skydeck-table-booking-antigravity-prompt.md">
# SkyDeck Table Booking Demo — Build Spec for Antigravity

## READ THIS FIRST (guardrails — do not skip)
Build **exactly** what is specified below. Do not add features, pages, auth systems, notifications, payments, or "nice to haves" beyond what's listed. Do not ask clarifying questions — every decision has already been made below; if something is ambiguous, pick the simplest option that satisfies the acceptance criteria at the bottom. Stop building once the acceptance criteria are met. This is a demo for tomorrow, not a production system — favor speed and a working end-to-end flow over polish in the backend.

---

## 1. Goal
A no-login table-status and booking demo for **SkyDeck Rooftop Restaurant** (rooftop + indoor AC + outdoor + banquet venue, Pimpri-Chinchwad, Pune). Two pages only:
1. **Customer page** (`/`) — scanned via QR, shows live table grid, lets a guest request a table.
2. **Admin page** (`/admin`) — staff see live requests + can manage table status.

Both pages must reflect the **same live data** — if a customer requests a table, the admin sees it appear within a few seconds without refreshing, and if admin changes a table's status, the customer grid updates too.

---

## 2. Tech Stack (fixed — do not substitute)
- **Next.js 14 (App Router)** + TypeScript
- **Tailwind CSS** for styling
- **Supabase** (free tier) — Postgres for data + Supabase Realtime for live sync across devices. This replaces the need for a custom backend/server.
- **lucide-react** for icons
- Deploy target: **Vercel**, with env vars for Supabase URL/key and an admin PIN.
- No other backend, no auth provider, no payment SDK, no SMS/WhatsApp integration.

---

## 3. Data Model (Supabase / Postgres)

```sql
create type table_status as enum ('available', 'pending', 'occupied', 'cleaning');
create type booking_status as enum ('pending', 'confirmed', 'cancelled', 'expired');

create table tables (
  id uuid primary key default gen_random_uuid(),
  label text not null,          -- e.g. "R4", "IN2", "O3"
  zone text not null,           -- 'Rooftop' | 'Indoor AC' | 'Outdoor'
  capacity int not null,
  status table_status not null default 'available',
  updated_at timestamptz not null default now()
);

create table bookings (
  id uuid primary key default gen_random_uuid(),
  table_id uuid references tables(id) not null,
  guest_name text not null,
  phone text not null,
  party_size int not null,
  note text,
  status booking_status not null default 'pending',
  created_at timestamptz not null default now()
);
```

For the demo, disable Row Level Security or set fully permissive policies (anon can select/insert/update on both tables). Add a code comment noting this is intentionally open for demo purposes and must be locked down before real production use.

Enable Supabase Realtime on both tables (Database → Replication) so client subscriptions work.

---

## 4. Seed Data
Seed 16 tables across three zones (banquet hall is intentionally excluded from this demo — it's normally booked as a whole event space, not a walk-in table; note this as an assumption in a code comment):

- **Rooftop**: R1–R6, capacities alternating 2 / 4 / 4 / 6 / 4 / 2
- **Indoor AC**: IN1–IN5, capacities 2 / 4 / 4 / 6 / 8
- **Outdoor**: O1–O5, capacities 2 / 4 / 4 / 6 / 4

All start as `available`.

---

## 5. Customer Page (`/`)

**Layout:**
- Header: "SkyDeck" wordmark + tagline "Live Table Availability"
- Tabs or section headers for the 3 zones (Rooftop / Indoor AC / Outdoor)
- Grid of table cards. Each card shows: table label, capacity (with a seat icon), and a colored status badge.

**Status colors:**
- Available → green
- Pending (someone mid-booking) → amber, label "Reserved — Confirming"
- Occupied → red
- Cleaning → gray/blue, label "Being Cleaned"

**Interaction:**
- Only `available` tables are tappable. Others show status only (no click action).
- Tapping an available table opens a bottom sheet (mobile) / modal (desktop) with a form:
  - Name (required, text)
  - Phone number (required, 10-digit validation, India format)
  - Party size (stepper, min 1, max = that table's capacity; if they need more, show a small note "For larger groups, please contact the desk")
  - Optional note (text, e.g. "birthday", "near the edge")
  - Submit button: "Request This Table"

**On submit:**
1. Client re-checks the table is still `available` (handle race condition: if someone else grabbed it first, show "Sorry, this table was just taken — please pick another" and refresh the grid instead of submitting).
2. Insert a `bookings` row with status `pending`.
3. Update the `tables` row to status `pending`.
4. Show a confirmation screen: a short booking reference (last 6 chars of the booking id), the message **"Please walk up to the reception desk to confirm your table within 10 minutes, or it will be released automatically."**, and a live 10-minute countdown.

**Auto-expiry:** After 10 minutes with no admin confirmation, the booking should flip to `expired` and the table back to `available`. Implement this as a simple client-side check on page load/interval (compare `created_at` + 10 min vs now) that updates the rows — no need for a server cron job for the demo; note in comments that a scheduled Supabase Edge Function would replace this in production.

---

## 6. Admin Page (`/admin`)

**Access:** No real auth system. A single PIN entry screen (PIN read from an env var, e.g. `ADMIN_PIN`) gates access, stored in a cookie/session for the browser tab. Add a code comment: *this is demo-only security, not production-grade.*

**Layout, two sections:**

1. **Live Table Grid** — same visual grid as the customer page, but every table (regardless of status) is clickable and cycles through: Available → Occupied → Cleaning → Available. This lets staff manually mark walk-ins or finished tables without going through the booking flow.

2. **Request Queue** — list of `bookings` with status `pending`, newest first, each showing: guest name, phone (tap-to-call link), party size, table label, note, time requested, and a live "time remaining" countdown. Two actions per row:
   - **Confirm & Seat** → booking status → `confirmed`, table status → `occupied`
   - **Release** → booking status → `cancelled`, table status → `available`

Also show a collapsed "Recent history" section below (last ~10 confirmed/cancelled/expired bookings) so staff have context, but keep it visually secondary.

All of this must update live via Supabase Realtime subscriptions — no manual refresh needed.

---

## 7. Design Direction
SkyDeck's own branding is a dark, upscale "rooftop at night" aesthetic. Match that:
- Near-black background (`#0F0F12` or similar) with a subtle warm gradient/glow, evoking a night skyline.
- Accent color: warm gold/amber (`#D4AF37`-ish) for highlights, buttons, and the "available" glow.
- Headings: an elegant serif (e.g. "Playfair Display") for the SkyDeck wordmark and section titles. Body/UI text: clean sans-serif (e.g. "Inter").
- Table cards: rounded corners, soft shadow, a colored left border or subtle glow matching status. Avoid flat/static "admin panel" look — this should feel like a boutique restaurant product, not internal tooling.
- Mobile-first: assume 90% of customer traffic is a phone right after a QR scan. Admin page can be a bit denser since staff will likely use a tablet/desktop.
- Keep animations minimal and purposeful (status change transitions, countdown ticking) — no heavy motion libraries needed; CSS transitions are enough.

---

## 8. Explicitly Out of Scope (do not build)
- Payments or deposits
- SMS/WhatsApp/email notifications
- Menu browsing or food ordering
- Multi-restaurant support
- Real staff accounts/roles (just the single shared PIN)
- Banquet hall event booking
- Waitlist/queue-position algorithm beyond the simple pending list

---

## 9. Deployment
- Push to a GitHub repo, deploy via Vercel.
- Env vars: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `ADMIN_PIN`.
- I will handle attaching the custom subdomain and generating the QR code myself — just make sure the customer page works standalone at the root `/`.

---

## 10. Acceptance Criteria (demo script)
1. Open `/` on a phone → see the 16-table grid grouped by zone, all green/available.
2. Tap an available table → fill form → submit → see confirmation + 10-min countdown; table turns amber on the grid.
3. Open `/admin` on a laptop (enter PIN) → see the new request appear in the queue within a few seconds, without refreshing.
4. Click "Confirm & Seat" → table turns red/occupied on both the admin grid and the customer page (open in another tab) within a few seconds.
5. On admin grid, click an occupied table → cycles to "Cleaning" → cycles to "Available" — reflected live on customer page.
6. Try booking an already-pending table from a second browser tab → get the "just taken" message, no duplicate booking created.
7. Leave a booking unconfirmed for 10+ minutes → it auto-expires and the table returns to available.
</file>

<file path="tailwind.config.ts">
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        gold: {
          DEFAULT: '#D4AF37',
          50: '#FAF5E4',
          100: '#F5EBC9',
          200: '#EBD793',
          300: '#E1C35D',
          400: '#D4AF37',
          500: '#B8962E',
          600: '#9C7D25',
          700: '#80641C',
          800: '#644B13',
          900: '#48320A',
        },
      },
      fontFamily: {
        serif: ['Playfair Display', 'Georgia', 'serif'],
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
      },
      animation: {
        'slide-up': 'slide-up 0.35s cubic-bezier(0.16, 1, 0.3, 1)',
      },
      keyframes: {
        'slide-up': {
          from: { transform: 'translateY(100%)', opacity: '0' },
          to: { transform: 'translateY(0)', opacity: '1' },
        },
      },
    },
  },
  plugins: [],
};

export default config;
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
</file>

</files>
